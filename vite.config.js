// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // Especifica la carpeta base para la aplicación (opcional, por defecto es '/')
  base: ''

  // Configuración específica para el entorno de desarrollo
 
});